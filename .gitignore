deps/usr/lib/
deps/build.log
build/
*.dylib
*.so
*.dll
# Manifest.toml is NOT tracked — each Julia version resolves its own.
# Artifacts.toml is intentionally tracked in git (not ignored here).
Manifest.toml

# Claude Code working directory
.claude/
